// // This file is required by Karma and should not be modified.

// // These are the polyfills needed for the test environment to run properly.
// import 'zone.js/dist/zone';
// import 'zone.js/dist/zone-testing';

// // This ensures that all files ending in ".spec.ts" are included in the test suite.
// const context = require.context('./', true, /\.spec\.ts$/);
// context.keys().map(context);

// // This is used to prevent any default Angular global configuration to interfere with the tests.
// // It's recommended to keep this line at the end of the file.
// // Alternatively, you can remove it if you don't need it.
// import { getTestBed } from '@angular/core/testing';
// import { BrowserDynamicTestingModule, platformBrowserDynamicTesting } from '@angular/platform-browser-dynamic/testing';

// getTestBed().initTestEnvironment(
//   BrowserDynamicTestingModule,
//   platformBrowserDynamicTesting()
// );
import 'jest-preset-angular';